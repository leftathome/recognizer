apiVersion: v1
kind: ConfigMap
metadata:
  name: relay-config
  namespace: capture
  labels:
    app: notification-relay
    app.kubernetes.io/part-of: capture-framework
data:
  config.yaml: |
    destinations: []
    # Example:
    # destinations:
    #   - name: discord
    #     url: https://placeholder.example.com
    #     url_env: DISCORD_WEBHOOK_URL
    #   - name: pushover
    #     url: https://placeholder.example.com
    #     url_env: PUSHOVER_API_URL
